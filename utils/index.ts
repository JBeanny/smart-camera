import {
  takePhoto,
  toggleCamera,
  downloadPhoto,
  discardPhoto,
  changeLensMode,
} from "./CameraActions";

const camera = {
  takePhoto,
  toggleCamera,
  downloadPhoto,
  discardPhoto,
  changeLensMode,
};

export { camera };
