import {
  takePhoto,
  toggleCamera,
  downloadPhoto,
  discardPhoto,
} from "./CameraActions";

const camera = {
  takePhoto,
  toggleCamera,
  downloadPhoto,
  discardPhoto,
};

export { camera };
